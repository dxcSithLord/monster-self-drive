# MonsterBorg Self-Drive Project Requirements
# Python 3.13.5+ (Debian Trixie default)
# Target: Raspberry Pi 3B with Raspberry Pi OS Trixie / Ubuntu 24.04+

# Core Dependencies
numpy>=2.0.0  # NumPy 2.x compatible (no deprecated APIs used)
opencv-python>=4.10.0
opencv-contrib-python>=4.10.0  # For extended tracking algorithms

# Raspberry Pi Camera Support (install separately on Pi)
# These require system dependencies - install with:
#   sudo apt install libcap-dev python3-picamera2 python3-libcamera
# Then: pip install picamera2
# Note: picamera is for older camera stack, picamera2 is for libcamera (newer)
# picamera>=1.13; platform_machine=="armv7l" or platform_machine=="aarch64"
# picamera2>=0.3.16; platform_machine=="armv7l" or platform_machine=="aarch64"

# Alternative: For development on non-Pi systems
# opencv-python will use regular webcam

# Web Server & Communication
Flask>=3.1.0  # Web framework (used in monsterWeb.py)
Flask-SocketIO>=5.5.0  # Real-time WebSocket communication (ADR-001)
python-socketio>=5.15.0  # Socket.IO protocol implementation
# Note: Flask-SocketIO supports threading, eventlet, or gevent async modes

# Hardware Interface (I2C for ThunderBorg and IMU)
smbus2>=0.4.3  # I2C communication (Python 3 compatible)

# Optional: IMU/Sensor Support
adafruit-circuitpython-mpu6050>=1.2.0  # MPU6050 IMU module
adafruit-circuitpython-bno055>=1.6.0  # BNO055 IMU (alternative)

# Optional: Machine Learning
# NOTE: No ML inference code implemented yet. When adding on-device inference:
# - ARM (aarch64): ai-edge-litert (LiteRT, formerly TensorFlow Lite)
# - x86_64 dev: tensorflow + keras (ensure keras>=3.12.0 for CVE fixes)
# See: https://ai.google.dev/edge/litert/migration

# Optional: Advanced Object Detection
# onnxruntime>=1.16.0  # ONNX Runtime - uncomment when inference code added

# Utilities
Pillow>=11.0.0  # Image processing utilities
scipy>=1.14.0  # Scientific computing (for filtering, signal processing)

# Development Dependencies (not needed on Pi)
pytest>=9.0.0  # Testing framework (requires Python 3.10+)
pytest-asyncio>=0.24.0  # Async testing
black>=24.0.0  # Code formatting
flake8>=7.0.0  # Linting
mypy>=1.8.0  # Type checking

# Documentation
sphinx>=7.2.0  # Documentation generation
sphinx-rtd-theme>=2.0.0  # Read the Docs theme

# Version Control Helpers
pre-commit>=3.6.0  # Git hooks for code quality
filelock>=3.20.1  # Transitive dep; CVE-2025-68146 fix (TOCTOU symlink vulnerability)
